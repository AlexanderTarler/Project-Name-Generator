{
  "name": "project-namer",
  "version": "1.0.0",
  "description": "",
  "main": "build/server.js",
  "scripts": {
    "start": "npm run build && nodemon build/server.js",
    "start:dev": "npx nodemon",
    "build": "rimraf ./build && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Johan Arkad, Alexander Tarler",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "rimraf": "^4.1.2",
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/mocha": "^10.0.1",
    "@types/node": "^18.13.0"
  }
}
